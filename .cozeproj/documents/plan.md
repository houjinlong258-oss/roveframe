# RoveFrame AI Business OS — 完整版实施计划

## 概述

面向**海外市场**中小企业的 **AI COO 智能经营平台**（Web 端完整版，非 Demo）。一体化架构：Next.js 单应用承载全部功能，**Supabase**（PostgreSQL + pgvector）作为数据库与向量存储，**平台 LLM 能力**（流式对话、内容生成）与**平台 Embedding 能力**（知识库向量化）作为 AI 底座。内置餐厅行业完整示例数据（菜单、订单、客户、评论、邮件、知识文档、预约），所有 AI 功能基于真实业务数据运行，一键部署即可投入使用。

**多语言与本地化**：基于 next-intl 全站 i18n，界面语言 **English 默认** / 中文 / Español，支持货币（USD 默认）与时区设置；AI 回复默认自动跟随客户语言（英文评论回英文）。核心业务闭环：**数据 → 知识 → 智能 → 行动**。

## 技术方案

| 维度 | 选择 | 理由 |
|------|------|------|
| 框架 | Next.js 16 (App Router) + React 19 + TS | 已初始化，前后端一体（API Routes） |
| UI | shadcn/ui + Tailwind CSS 4 | 项目预装，企业级 Dashboard 体系 |
| 数据库 | Supabase (PostgreSQL + pgvector) | 结构化业务数据 + 向量检索一体化，平台托管集成 |
| AI 对话/生成 | 平台 LLM 能力（流式 SSE） | AI COO Agent、内容生成，流式优先 |
| 向量化 | 平台 Embedding 能力 | Knowledge Brain 文档 RAG |
| 图表 | Recharts | Dashboard 营收/订单/趋势可视化 |
| 多语言 | next-intl（en 默认 / zh / es） | 面向海外市场，文案全外置，货币+时区本地化 |
| 示例数据 | 餐厅行业 seed 数据 | 蓝图首选行业，开箱即用，可清空替换 |

## 功能模块

### 1. 数据层（Supabase 11 张表）

| 表 | 关键字段 | 说明 |
|----|---------|------|
| products | id, name, category, price, cost, description, status, sales_count | 菜单/产品 |
| orders | id, order_no, customer_id, items(jsonb), total, status, channel, created_at | 订单流水 |
| customers | id, name, phone, email, tags[], total_spent, visit_count, last_visit, ai_score, churn_risk | 客户 360 |
| reviews | id, platform, author, rating, content, sentiment, ai_reply, replied_at | 多平台评论 |
| emails | id, from_addr, subject, body, category, priority, ai_summary, draft_reply, status | 邮件智能 |
| knowledge_docs | id, title, content, category, embedding(vector) | 知识库文档 |
| chat_sessions / chat_messages | 会话与消息（role, content, created_at） | AI COO 对话持久化 |
| alerts | id, type, title, content, severity, read, created_at | 实时告警 |
| marketing_contents | id, type, title, content, status, created_at | 营销内容资产 |
| model_configs | id, provider, api_key_encrypted, base_url, model, status, route_assignments(jsonb) | 用户自接 AI 模型（Key 加密存储） |
| reservations | id, customer_name, phone, party_size, table_no, reserved_at, duration, source, status(待确认/已确认/已到店/已取消/未到店), notes | 餐厅预约与桌位 |

### 2. AI 能力层（API Routes，全部真实调用）

**模型路由**：所有 AI 调用统一经过模型路由层——用户已在设置中接入自己的大模型（**Claude / Anthropic（主力，原生 Messages 协议）**、OpenAI、Gemini、DeepSeek、豆包、Kimi、通义千问、智谱 GLM、Grok、自定义 OpenAI 兼容接口）时优先调用用户模型（流式 SSE），未接入时回落平台内置 LLM 能力。路由层内置各服务商预设（默认 Base URL + 模型列表），Claude 走 Anthropic Messages 协议适配，其余统一走 OpenAI 兼容协议。

| API | 能力 |
|-----|------|
| `/api/agent/chat` | AI COO 流式对话：自动拉取营收/订单/客户摘要注入上下文，回答经营问题并给出行动建议 |
| `/api/insights` | Dashboard AI 洞察：基于真实数据生成经营解读与异常提示 |
| `/api/knowledge` | 文档 CRUD + 上传即向量化（embedding 入库） |
| `/api/knowledge/ask` | RAG 问答：向量检索相关文档 → LLM 生成带依据的回答 |
| `/api/reviews/analyze` | 评论情感分析 + AI 回复生成（可批量） |
| `/api/emails/classify` | 邮件分类、优先级判定、摘要、回复草稿生成 |
| `/api/marketing/generate` | 营销活动创意 / 社媒文案 / 邮件营销内容生成 |
| `/api/customers/score` | 客户价值评分与流失风险分析 |
| `/api/settings/models` | 模型服务商配置 CRUD（GET 仅回传 Key 掩码，PUT 加密写入）+ 按能力分配模型 |
| `/api/settings/models/test` | 测试连接：服务端用所填 Key/Base URL/模型发起最小化请求，返回连通性与延迟 |

业务 CRUD（非 AI，REST）：`/api/products`、`/api/orders`、`/api/reservations`（预约含桌位占用冲突校验）、`/api/settings`（业务信息、语言/货币/时区、AI 偏好）。

### 3. 页面模块

10 个页面：经营仪表盘、AI COO 助手、预约管理、知识大脑、评论智能、客户智能、营销增长、邮件中心、经营数据（产品+订单）、设置（含 AI 模型接入、语言与地区）。详见页面规格。

## 是否有原型设计

是

## 实施步骤

### 阶段一：原型设计

1. **原型设计**：加载 design-canvas 技能，按页面规格完成全局导航 + 10 个页面的 HTML 原型（Tailwind v4 + Lucide），提交用户验收；确认后进入开发阶段。

### 阶段二：代码开发

2. **基础设施**：Supabase 建 11 张表 + 餐厅示例数据 seed + AI 集成封装（模型路由层：用户自接模型优先/平台 LLM 兜底、embedding 客户端、业务上下文构建器）+ next-intl i18n 脚手架（en/zh/es 字典、默认英文、货币与时区格式化工具）—— `src/lib/db.ts`、`src/lib/ai.ts`、`src/lib/business-context.ts`
3. **全局布局 + 经营仪表盘**：侧边导航布局、KPI 卡片、营收/订单/客流图表、AI 洞察流、实时告警中心 —— `src/app/layout.tsx`、`src/app/page.tsx`、`src/app/api/insights/route.ts`
4. **AI COO 助手**：流式对话界面（SSE）、业务上下文注入、会话列表与持久化、快捷问题模板 —— `src/app/agent/page.tsx`、`src/app/api/agent/chat/route.ts`
5. **知识大脑**：文档管理（新建/编辑/删除/分类）、上传自动向量化、RAG 问答界面（引用来源展示）—— `src/app/knowledge/page.tsx`、`src/app/api/knowledge/route.ts`
6. **评论智能 + 客户智能**：多平台评论列表、情感分析、AI 回复一键生成与标记；客户列表、360 抽屉（消费画像/AI 评分/流失预警）、批量评分 —— `src/app/reviews/page.tsx`、`src/app/customers/page.tsx`
7. **营销增长 + 邮件中心**：营销内容生成工作台（活动创意/社媒/邮件三类，保存为资产）；邮件收件箱、AI 分类与优先级、摘要与回复草稿 —— `src/app/marketing/page.tsx`、`src/app/emails/page.tsx`
8. **预约管理 + 经营数据 + 设置 + 收尾验证**：预约管理（预订时间线/桌位状态板/新建预约/状态流转）、产品/订单管理（Tab 页）、设置页（AI 模型接入/业务信息/语言与地区/AI 偏好/数据管理）、原型一致性检查、test_run 全量验收 —— `src/app/reservations/page.tsx`、`src/app/business/page.tsx`、`src/app/settings/page.tsx`

## 页面规格

##### @nav(web-topbar)
> type: topbar
> platform: web

- @page(/) 经营仪表盘
- @page(/agent) AI COO 助手
- @page(/reservations) 预约管理
- @page(/knowledge) 知识大脑
- @page(/reviews) 评论智能
- @page(/customers) 客户智能
- @page(/marketing) 营销增长
- @page(/emails) 邮件中心
- @page(/business) 经营数据

### 页面详情

##### @page(/) 经营仪表盘

**核心职责**：一屏呈现餐厅经营全貌与 AI 生成的经营洞察。
**访问路径**：导航直达，Logo 点击回到本页。
**布局**：顶部导航栏（Logo + 导航项 + 告警铃铛 + 设置入口）；主内容区自上而下：KPI 卡片区（今日营收/今日订单/客流量/好评率）→ 图表区（近 7 日营收趋势折线图 + 热销菜品 Top5 横向柱状图 + 订单渠道占比饼图）→ 下半区左右分栏：AI 经营洞察流（左，卡片流，含正/负/建议三类标记）+ 实时告警列表（右）。
**列表项字段（告警）**：严重程度图标 / 标题 / 摘要 / 时间
**状态**：
- 加载态：KPI 与图表骨架屏
- 错误态：图表区显示重试按钮

**交互说明**

| 元素 | 动作 | 响应 | 传参 | 备注 |
|------|------|------|------|------|
| Logo | 点击 | 跳转 @page(/) | — | — |
| 告警铃铛 | 点击 | 展开告警下拉面板；点击单条标记已读 | — | 未读数角标 |
| AI 洞察卡片 | 点击"问 AI" | 跳转 @page(/agent) 并自动带入该洞察作为提问 | insight | — |
| 时间范围切换 | 点击 7天/30天 | 刷新图表与 KPI | — | — |
| 设置入口 | 点击 | 跳转 @page(/settings) | — | 头像/齿轮 |

##### @page(/agent) AI COO 助手

**核心职责**：通过自然对话获取经营分析、策略建议与可执行行动方案（流式输出）。
**访问路径**：导航直达；Dashboard 洞察卡片"问 AI"带参进入。
**布局**：顶部导航栏；左侧会话列表栏（历史会话 + 新建对话按钮）；右侧对话区：消息流（用户右/AI 左，AI 消息支持 Markdown 渲染与逐字流式）+ 底部输入框（多行、发送按钮、快捷问题模板 chips：如"本周营收怎么样""帮我提升周末客流"）。
**列表项字段（会话）**：会话标题 / 最后消息时间
**状态**：
- 空态：居中欢迎语 + 6 个快捷问题卡片
- 生成中：AI 气泡打字光标 + 停止生成按钮

**交互说明**

| 元素 | 动作 | 响应 | 传参 | 备注 |
|------|------|------|------|------|
| Logo | 点击 | 跳转 @page(/) | — | — |
| 新建对话 | 点击 | 创建空会话并选中 | — | — |
| 会话项 | 点击 | 加载该会话历史消息 | session_id | — |
| 发送按钮/Enter | 点击 | 用户消息上屏，SSE 流式渲染 AI 回复 | message, session_id | Shift+Enter 换行 |
| 快捷问题 chip | 点击 | 自动填入输入框并发送 | — | — |
| 停止生成 | 点击 | 中断流式输出 | — | 仅生成中可见 |
| 删除会话 | 点击图标 | 弹窗 @modal(delete-session) 确认后删除 | session_id | 悬停显示 |

**弹窗 delete-session**：
- 标题："删除对话"
- 内容：该会话的标题
- 操作：确认（删除并刷新列表）、取消（关闭）

##### @page(/reservations) 预约管理

**核心职责**：餐厅预约统一管理——预订时间线、桌位占用状态与预约状态流转。
**访问路径**：侧边导航直达。
**布局**：顶部导航栏；统计条（今日预约/待确认/已到店/取消率）；日期周条（今天高亮，可切换前后日期）；主区左右分栏：左（约 2/3）预约时间线列表（按时段分组，卡片含时间/姓名/人数/桌位/来源/状态徽章），右（约 1/3）桌位状态板（桌位网格，空闲/已订/用餐中三色）。
**列表项字段**：时间 / 姓名 / 人数 / 桌位 / 来源 / 联系电话 / 状态
**状态**：
- 空态：所选日期暂无预约
- 冲突：新建时同桌位同时段已被占用，表单内提示

**交互说明**

| 元素 | 动作 | 响应 | 传参 | 备注 |
|------|------|------|------|------|
| Logo | 点击 | 跳转 @page(/) | — | — |
| 日期周条 | 点击日期 | 刷新时间线与桌位状态板 | date | 今天默认选中 |
| 新建预约 | 点击 | 弹窗 @modal(reservation-form)（姓名/电话/人数/日期时间/桌位/备注） | — | — |
| 预约卡片 | 点击 | 弹窗 @modal(reservation-detail) 查看并流转状态 | reservation_id | — |
| 桌位格 | 点击 | 按桌位筛选当日预约 | table_no | 再点取消筛选 |

**弹窗 reservation-form**：
- 字段：姓名、电话、人数、日期、时间、桌位（下拉，占用中置灰）、备注
- 操作：保存（校验同桌同时段冲突）、取消

**弹窗 reservation-detail**：
- 内容：完整预约信息、来源、状态
- 操作：状态流转（确认/到店/取消/未到店）、关闭

##### @page(/knowledge) 知识大脑

**核心职责**：管理业务知识文档（SOP/菜单知识/政策），并通过 RAG 问答让 AI 基于知识回答。
**访问路径**：导航直达。
**布局**：顶部导航栏；上部为 RAG 问答区（输入框 + 回答卡片，回答下方列出引用来源文档）；下部为文档管理区：分类 Tab（全部/运营 SOP/产品知识/客户政策）+ 文档卡片网格 + 新建文档按钮。
**列表项字段（文档卡片）**：标题 / 分类标签 / 摘要 / 更新时间 / 向量化状态点
**状态**：
- 空态：引导创建第一篇文档
- 问答生成中：回答卡片骨架屏

**交互说明**

| 元素 | 动作 | 响应 | 传参 | 备注 |
|------|------|------|------|------|
| Logo | 点击 | 跳转 @page(/) | — | — |
| 问答输入框 | 回车 | 流式生成回答并展示引用文档 | question | — |
| 新建文档 | 点击 | 弹窗 @modal(doc-form)（标题/分类/内容） | — | — |
| 文档卡片 | 点击 | 弹窗 @modal(doc-form) 编辑模式 | doc_id | — |
| 删除文档 | 点击图标 | 弹窗 @modal(doc-delete) 确认后删除并移除向量 | doc_id | — |
| 分类 Tab | 点击 | 按分类筛选文档列表 | category | — |

**弹窗 doc-form**：
- 字段：标题、分类（下拉）、内容（多行 Markdown）
- 操作：保存（新增/更新并触发向量化）、取消

**弹窗 doc-delete**：
- 标题："删除文档"；内容：文档标题；操作：确认、取消

##### @page(/reviews) 评论智能

**核心职责**：聚合多平台顾客评论，AI 情感分析并一键生成回复。
**访问路径**：导航直达。
**布局**：顶部导航栏；统计条（平均分/好评率/待回复数/情感分布迷你条）；筛选行（平台下拉：全部/Google/Yelp/Facebook + 情感下拉 + 仅看待回复开关）；评论卡片列表（每条含平台徽标、星级、正文、情感标签、AI 回复区）。
**列表项字段**：平台 / 作者 / 星级 / 内容摘要 / 情感标签 / 时间 / 回复状态
**状态**：
- 空态：暂无评论提示
- 分析中：对应卡片情感标签处 loading

**交互说明**

| 元素 | 动作 | 响应 | 传参 | 备注 |
|------|------|------|------|------|
| Logo | 点击 | 跳转 @page(/) | — | — |
| 平台/情感筛选 | 选择 | 刷新列表 | platform, sentiment | — |
| 仅看待回复 | 开关 | 过滤未回复评论 | — | — |
| 生成 AI 回复 | 点击 | 流式生成回复填入回复区 | review_id | 未回复时显示 |
| 重新生成 | 点击 | 重新生成回复 | review_id | 已有回复时显示 |
| 标记已回复 | 点击 | 更新状态并刷新统计 | review_id | — |
| 批量分析 | 点击 | 对全部未分析评论执行情感分析 | — | 顶部按钮 |

##### @page(/customers) 客户智能

**核心职责**：客户资产总览，AI 评分与流失预警，辅助留存决策。
**访问路径**：导航直达。
**布局**：顶部导航栏；统计条（总客户/本月新增/平均客单价/高流失风险数）；工具行（搜索框 + 标签筛选 + 批量 AI 评分按钮）；客户表格（行点击展开右侧 360 抽屉）。
**列表项字段**：姓名 / 标签 / 消费总额 / 到店次数 / 最近到店 / AI 评分（进度条） / 流失风险徽章
**状态**：
- 空态：暂无客户
- 评分中：评分列 loading

**交互说明**

| 元素 | 动作 | 响应 | 传参 | 备注 |
|------|------|------|------|------|
| Logo | 点击 | 跳转 @page(/) | — | — |
| 搜索框 | 输入 | 按姓名/手机号实时过滤 | keyword | 防抖 |
| 客户行 | 点击 | 右侧滑出客户 360 抽屉 | customer_id | — |
| 批量 AI 评分 | 点击 | 对全部客户计算评分与流失风险并刷新 | — | — |
| 抽屉内"生成挽留方案" | 点击 | 生成该客户的个性化挽留建议（抽屉内流式展示） | customer_id | 仅高流失风险显示 |

**抽屉 customer-360**：
- 内容：基础信息、消费统计（总额/次数/客单价/最近到店）、标签、AI 评分解读、流失风险分析、历史订单简表
- 操作：关闭、"生成挽留方案"（高风险时）

##### @page(/marketing) 营销增长

**核心职责**：基于经营数据 AI 生成营销创意与内容，并保存为内容资产。
**访问路径**：导航直达。
**布局**：顶部导航栏；左侧生成工作台：内容类型选择（活动策划/社交媒体/邮件营销）+ 目标描述输入框 + 生成按钮 + 生成结果区（流式，支持 Markdown）+ 保存按钮；右侧内容资产列表（卡片流，含类型标签与状态）。
**列表项字段（资产卡片）**：类型图标 / 标题 / 摘要 / 状态标签（草稿/已采用） / 创建时间
**状态**：
- 空态：资产区显示"还没有营销内容"
- 生成中：结果区流式打字

**交互说明**

| 元素 | 动作 | 响应 | 传参 | 备注 |
|------|------|------|------|------|
| Logo | 点击 | 跳转 @page(/) | — | — |
| 内容类型 | 点击切换 | 更新生成表单提示语 | type | — |
| 生成按钮 | 点击 | 流式生成内容到结果区 | type, brief | — |
| 保存为资产 | 点击 | 存入资产列表 | content | 生成完成后可用 |
| 资产卡片 | 点击 | 弹窗 @modal(content-view) 查看全文 | content_id | — |
| 标记已采用 | 点击 | 更新资产状态 | content_id | 弹窗内操作 |
| 删除资产 | 点击 | 弹窗 @modal(content-delete) 确认后删除 | content_id | — |

**弹窗 content-view**：
- 内容：标题、类型、全文（Markdown 渲染）
- 操作：标记已采用/取消采用、复制、关闭

**弹窗 content-delete**：
- 标题："删除内容"；操作：确认、取消

##### @page(/emails) 邮件中心

**核心职责**：邮件智能收件箱——AI 自动分类、判优先级、写摘要与回复草稿。
**访问路径**：导航直达。
**布局**：顶部导航栏；三栏式：左栏分类列表（全部/客户咨询/潜在商机/投诉/供应商/其他，带未读数）；中栏邮件列表（发件人/主题/AI 摘要/优先级徽章）；右栏详情区（原文、AI 摘要、分类与优先级、回复草稿编辑框、操作按钮）。
**列表项字段**：发件人 / 主题 / AI 摘要 / 优先级徽章（高/中/低） / 时间 / 状态
**状态**：
- 空态：分类下无邮件
- 分析中：详情区 AI 区块骨架屏

**交互说明**

| 元素 | 动作 | 响应 | 传参 | 备注 |
|------|------|------|------|------|
| Logo | 点击 | 跳转 @page(/) | — | — |
| 分类项 | 点击 | 刷新中栏列表 | category | — |
| 邮件项 | 点击 | 右栏展示详情并标记已读 | email_id | — |
| AI 分析 | 点击 | 对该邮件执行分类/优先级/摘要/草稿生成 | email_id | 未分析时显示 |
| 重新生成草稿 | 点击 | 重新生成回复草稿 | email_id | — |
| 草稿编辑框 | 输入 | 编辑草稿内容 | — | — |
| 标记已处理 | 点击 | 更新状态并刷新列表 | email_id | — |
| 全部批量分析 | 点击 | 对所有未分析邮件执行 AI 分析 | — | 中栏顶部 |

##### @page(/business) 经营数据

**核心职责**：产品与订单的统一管理后台（餐厅菜单 + 订单流水）。
**访问路径**：导航直达。
**布局**：顶部导航栏；Tab 切换（产品管理 / 订单管理）。
- 产品 Tab：分类筛选 chips + 新建产品按钮 + 产品表格（图片占位/名称/分类/价格/成本/销量/状态开关）
- 订单 Tab：状态筛选（全部/待处理/进行中/已完成/已取消）+ 订单表格（单号/客户/金额/渠道/状态徽章/时间）

**列表项字段（产品）**：名称 / 分类 / 价格 / 成本 / 销量 / 状态
**列表项字段（订单）**：订单号 / 客户名 / 金额 / 渠道 / 状态 / 时间
**状态**：
- 空态：对应 Tab 无数据提示

**交互说明**

| 元素 | 动作 | 响应 | 传参 | 备注 |
|------|------|------|------|------|
| Logo | 点击 | 跳转 @page(/) | — | — |
| Tab 切换 | 点击 | 切换产品/订单视图 | — | — |
| 新建产品 | 点击 | 弹窗 @modal(product-form) | — | — |
| 产品行编辑 | 点击 | 弹窗 @modal(product-form) 编辑模式 | product_id | — |
| 产品状态开关 | 切换 | 上架/下架并刷新 | product_id | — |
| 订单行 | 点击 | 抽屉展示订单详情（商品明细/客户信息） | order_id | — |
| 订单状态徽章 | 点击 | 下拉切换状态（待处理→进行中→已完成） | order_id, status | — |

**弹窗 product-form**：
- 字段：名称、分类（下拉）、价格、成本、描述、状态
- 操作：保存、取消

**抽屉 order-detail**：
- 内容：订单号、时间、渠道、客户信息、商品明细表（名称/数量/单价/小计）、合计、状态
- 操作：状态推进、关闭

##### @page(/settings) 设置

**核心职责**：AI 模型接入、业务信息配置、语言与地区、AI 行为偏好与数据管理。
**访问路径**：Dashboard 顶栏设置入口进入。
**布局**：顶部导航栏；左侧设置分组菜单（AI 模型接入 / 业务信息 / 语言与地区 / AI 偏好 / 数据管理）；右侧对应面板区。
- AI 模型接入：服务商卡片网格（**Claude（Anthropic，主力）** / OpenAI / Gemini / DeepSeek / 豆包 / Kimi / 通义千问 / 智谱 GLM / Grok / 自定义 OpenAI 兼容接口，各含连接状态与配置入口）+ 模型分配区（为 AI 对话/内容生成/RAG 问答分别选择已接入模型或平台内置模型）
- 业务信息：店名/行业/规模/营业时间/简介表单
- 语言与地区：界面语言（**English 默认** / 中文 / Español）、货币（USD 默认 / EUR / GBP / CNY）、时区、AI 回复语言策略（跟随客户语言 / 固定语言）
- AI 偏好：回复风格、自动化开关组
- 数据管理：数据概况 + 清空示例数据危险区

**状态**：保存成功显示 Toast；模型测试连接显示成功/失败与延迟

**交互说明**

| 元素 | 动作 | 响应 | 传参 | 备注 |
|------|------|------|------|------|
| Logo | 点击 | 跳转 @page(/) | — | — |
| 分组菜单 | 点击 | 切换右侧面板 | — | — |
| 服务商卡片"配置/管理" | 点击 | 弹窗 @modal(model-config)（API Key/Base URL/默认模型） | provider | Key 仅显示掩码 |
| 测试连接 | 点击 | 服务端真实验证连通性，显示结果与延迟 | provider, api_key, base_url, model | 弹窗内 |
| 模型配置保存 | 点击 | 加密保存 Key，卡片状态更新为已连接 | provider 配置 | 弹窗内 |
| 模型分配保存 | 点击 | 保存各 AI 能力使用的模型来源 | route_assignments | — |
| 业务信息保存 | 点击 | 保存店名/行业/规模/营业时间 | — | — |
| 语言与地区保存 | 点击 | 保存语言/货币/时区/AI 回复语言并全局生效（界面文案即时切换） | locale, currency, timezone | — |
| AI 偏好保存 | 点击 | 保存回复风格/自动化开关 | — | — |
| 清空示例数据 | 点击 | 弹窗 @modal(wipe-data) 二次确认后清空业务表 | — | 危险操作，红色按钮 |

**弹窗 model-config**：
- 字段：API Key（密码框，已配置显示掩码）、Base URL（按服务商预填默认值）、默认模型（下拉）
- 操作：测试连接（显示成功/失败+延迟）、保存、取消

**弹窗 wipe-data**：
- 标题："确认清空全部数据"
- 内容：说明将删除产品/订单/客户/评论/邮件等全部数据且不可恢复
- 操作：确认清空（红色）、取消
