docs: pilot-ready status, gap plan status, README repositioning

- PRODUCTION_GAP_PLAN.md: implementation status table (P0-1..P0-6 done
  with commits) + external deployment prerequisites
- docs/current/PILOT_READY_STATUS.md: acceptance checklist, deployment
  steps (migrations, env, merchant onboarding), validation matrix,
  frozen scope
- README: repositioned as RoveFrame AI Executive Operations Platform /
  Restaurant AI Chief of Staff with the real closed loop and three
  core entries

Validation: pnpm validate (ts-check + eslint + stylelint + 278 TS tests +
production scan) and 21 Python tests all green.